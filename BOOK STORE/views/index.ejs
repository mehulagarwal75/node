<!DOCTYPE html>
<html>
<head>
    <title>Book Store Inventory</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* --- RESET & BASIC STYLES --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f7f6;
            color: #333;
            padding: 20px;
        }

        /* --- HEADER AREA --- */
        .header-container {
            max-width: 1200px;
            margin: 0 auto 30px auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: white;
            padding: 20px 30px;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }

        h1 {
            font-size: 24px;
            color: #2c3e50;
        }

        .btn-add {
            background-color: #2ecc71;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: bold;
            transition: background 0.3s;
        }
        .btn-add:hover { background-color: #27ae60; }

        /* --- GRID LAYOUT --- */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 25px;
            max-width: 1200px;
            margin: 0 auto;
        }

        /* --- BOOK CARD --- */
        .card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            transition: transform 0.2s, box-shadow 0.2s;
            display: flex;
            flex-direction: column;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }

        .card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-bottom: 1px solid #eee;
        }

        .card-content {
            padding: 15px;
            flex-grow: 1; /* Pushes buttons to bottom */
            text-align: left;
        }

        .card h3 {
            font-size: 18px;
            margin-bottom: 5px;
            color: #2c3e50;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .author {
            color: #7f8c8d;
            font-size: 14px;
            margin-bottom: 10px;
        }

        .details {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
            margin-bottom: 15px;
            color: #555;
            background: #f8f9fa;
            padding: 8px;
            border-radius: 6px;
        }

        .price {
            font-weight: bold;
            color: #27ae60;
            font-size: 16px;
        }

        /* --- BUTTONS --- */
        .actions {
            padding: 15px;
            border-top: 1px solid #f0f0f0;
            display: flex;
            justify-content: space-between;
            gap: 10px;
        }

        .btn {
            flex: 1;
            text-align: center;
            padding: 8px 0;
            border-radius: 6px;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: background 0.2s;
        }

        .btn-edit {
            background-color: #3498db;
            color: white;
        }
        .btn-edit:hover { background-color: #2980b9; }

        .btn-delete {
            background-color: #e74c3c;
            color: white;
        }
        .btn-delete:hover { background-color: #c0392b; }

        /* --- EMPTY STATE --- */
        .empty-msg {
            grid-column: 1 / -1;
            text-align: center;
            padding: 50px;
            color: #95a5a6;
            font-size: 18px;
        }
    </style>
</head>
<body>

    <div class="header-container">
        <h1>📚 My Book Store Library</h1>
        <a href="/add" class="btn-add">+ Add New Book</a>
    </div>

    <div class="grid">
        <% if(books.length > 0) { %>
            <% books.forEach((book) => { %>
                <div class="card">
                    <img src="/uploads/<%= book.book_image %>" alt="<%= book.book_name %>">
                    
                    <div class="card-content">
                        <h3><%= book.book_name %></h3>
                        <p class="author">By <%= book.book_author %></p>
                        
                        <div class="details">
                            <span>Lang: <%= book.book_lang %></span>
                            <span class="price">$<%= book.book_price %></span>
                        </div>
                    </div>

                    <div class="actions">
                        <a href="/edit/<%= book._id %>" class="btn btn-edit">Edit</a>
                        <a href="/delete/<%= book._id %>" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this book?');">Delete</a>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="empty-msg">
                <p>No books found. Click "Add New Book" to start your collection!</p>
            </div>
        <% } %>
    </div>

</body>
</html>