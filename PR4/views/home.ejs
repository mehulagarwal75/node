<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bharat Law Directory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --navy: #0f172a; --gold: #c5a059; --bg: #f1f5f9; }
        body { background-color: var(--bg); font-family: 'Inter', sans-serif; }
        .hero { background: var(--navy); padding: 40px 0 80px; color: white; text-align: center; clip-path: polygon(0 0, 100% 0, 100% 85%, 0 100%); }
        .adv-card { background: white; border-radius: 15px; border: none; overflow: hidden; transition: 0.3s; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .adv-card:hover { transform: translateY(-5px); box-shadow: 0 15px 20px rgba(0,0,0,0.1); }
        
        /* UPDATED: CSS for the Placeholder Avatar instead of Image */
        .profile-placeholder { 
            width: 100%; 
            height: 180px; 
            background: #e2e8f0; /* Light gray background */
            display: flex;
            align-items: center;
            justify-content: center;
            color: #94a3b8; /* Icon color */
            font-size: 4rem; 
        }

        .role-pill { background: #fef3c7; color: #92400e; padding: 4px 12px; border-radius: 50px; font-size: 0.75rem; font-weight: 800; text-transform: uppercase; }
        .phone-btn { text-decoration: none; color: var(--navy); background: #f8fafc; display: block; padding: 8px; text-align: center; border-radius: 8px; font-weight: 600; margin-top: 10px; }
    </style>
</head>
<body>

    <div class="hero">
        <h2 class="fw-bold">BHARAT ADVOCATE DIRECTORY</h2>
        <p class="opacity-75">Admin Dashboard</p>
    </div>

    <div class="container" style="margin-top: -50px; position: relative; z-index: 5;">
        <div class="bg-white p-3 rounded-pill shadow d-flex align-items-center justify-content-between mb-5">
            <span class="ms-4 fw-bold text-muted">Manage Records</span>
            <a href="/addAdvocatePage" class="btn btn-dark rounded-pill px-4 fw-bold">
                <i class="fas fa-plus me-2"></i> ADD NEW ADVOCATE
            </a>
        </div>

        <div class="row g-4">
            <% advocates.forEach((adv) => { %>
                <div class="col-md-6 col-lg-4 col-xl-3">
                    <div class="adv-card h-100 d-flex flex-column">
                        
                        <div class="profile-placeholder">
                            <i class="fas fa-user-tie"></i>
                        </div>
                        
                        <div class="p-3 d-flex flex-column flex-grow-1">
                            <div class="mb-2"><span class="role-pill"><%= adv.role %></span></div>
                            <h5 class="fw-bold mb-1"><%= adv.name %></h5>
                            
                            <a href="tel:<%= adv.phone %>" class="phone-btn">
                                <i class="fas fa-phone-alt me-2"></i><%= adv.phone %>
                            </a>
                            
                            <p class="small text-muted mt-3 mb-3">
                                <i class="fas fa-map-marker-alt me-1"></i> <%= adv.address %>
                            </p>

                            <div class="mt-auto d-flex gap-2">
                                <a href="/editAdvocate/<%= adv.id %>" class="btn btn-outline-primary btn-sm flex-grow-1 rounded-pill fw-bold">EDIT</a>
                                <a href="/deleteAdvocate?id=<%= adv.id %>" class="btn btn-outline-danger btn-sm px-3 rounded-pill" onclick="return confirm('Delete this record?')">
                                    <i class="fas fa-trash"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
            
            <% if(advocates.length === 0) { %>
                <div class="text-center text-muted mt-5">
                    <h4>No records found.</h4>
                    <p>Click "Add New Advocate" to get started.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>