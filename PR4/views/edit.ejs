<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Advocate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>body { background-color: #f1f5f9; padding-top: 50px; }</style>
</head>
<body>
    <div class="container" style="max-width: 600px;">
        <div class="card shadow border-0 rounded-4">
            <div class="card-body p-5">
                <h3 class="fw-bold mb-4 text-center">Edit Profile</h3>
                
                <form action="/updateAdvocate" method="post" enctype="multipart/form-data">
                    <input type="hidden" name="id" value="<%= advocate.id %>">

                    <div class="mb-3 text-center">
                        <img src="/<%= advocate.adv_image %>" style="width: 100px; height: 100px; object-fit: cover; border-radius: 50%;">
                        <div class="mt-2">
                            <label class="fw-bold small text-muted">CHANGE PHOTO</label>
                            <input type="file" name="adv_image" class="form-control mt-1">
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="fw-bold small text-muted">FULL NAME</label>
                        <input type="text" name="name" value="<%= advocate.name %>" class="form-control" required>
                    </div>

                    <div class="row mb-3">
                        <div class="col-6">
                            <label class="fw-bold small text-muted">DESIGNATION</label>
                            <select name="role" class="form-select">
                                <option <%= advocate.role == 'Senior Advocate' ? 'selected' : '' %>>Senior Advocate</option>
                                <option <%= advocate.role == 'Advocate on Record' ? 'selected' : '' %>>Advocate on Record</option>
                                <option <%= advocate.role == 'Corporate Counsel' ? 'selected' : '' %>>Corporate Counsel</option>
                                <option <%= advocate.role == 'Criminal Defense' ? 'selected' : '' %>>Criminal Defense</option>
                                <option <%= advocate.role == 'Civil Lawyer' ? 'selected' : '' %>>Civil Lawyer</option>
                            </select>
                        </div>
                        <div class="col-6">
                            <label class="fw-bold small text-muted">PHONE</label>
                            <input type="number" name="phone" value="<%= advocate.phone %>" class="form-control" required>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="fw-bold small text-muted">ADDRESS</label>
                        <textarea name="address" class="form-control" rows="2" required><%= advocate.address %></textarea>
                    </div>

                    <div class="d-flex gap-2">
                        <a href="/" class="btn btn-light flex-grow-1 rounded-pill fw-bold">CANCEL</a>
                        <button type="submit" class="btn btn-primary flex-grow-1 rounded-pill fw-bold">UPDATE RECORD</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>