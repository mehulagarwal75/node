<!DOCTYPE html>
<html dir="ltr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Forgot Password | Jeen Creation</title>
    <link href="dist/css/style.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            height: 100vh;
            display: flex;
            align-items: center;
        }
        .auth-box {
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            padding: 30px;
        }
        .btn-custom {
            background: linear-gradient(to right, #667eea, #764ba2);
            border: none;
            color: white;
            border-radius: 25px;
            padding: 10px;
            transition: 0.3s;
        }
        .btn-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="auth-box">
                    <div class="text-center mb-4">
                        <h2 class="font-bold text-dark">FORGOT PASSWORD</h2>
                        <p class="text-muted">Enter your email to reset password</p>
                    </div>
                    <form action="/verify-email" method="POST">
                        <div class="form-group">
                            <label><i class="mdi mdi-email"></i> Email Address</label>
                            <input type="email" name="email" class="form-control" placeholder="example@mail.com" required>
                        </div>
                        <button type="submit" class="btn btn-custom btn-block mt-4">SEND OTP</button>
                    </form>
                    <div class="text-center mt-3">
                        <a href="/" class="text-muted">Back to Login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
